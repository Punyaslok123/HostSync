<%- include('../partials/head') %>
</head>
<body class="min-h-screen bg-gradient-to-br from-[#1f1c2c] via-[#2c3e50] to-[#4b6cb7] font-[Inter] text-white">


<script src="https://cdn.tailwindcss.com"></script>

<%- include('../partials/nav') %>

<main class="pt-32 pb-20 px-4 max-w-7xl mx-auto">
  <!-- Header -->
  <div class="mb-10 text-center">
    <h1 class="text-4xl md:text-5xl font-extrabold text-gradient shimmer">
      Stay at <%= home.houseName %>
    </h1>
    <p class="mt-3 text-lg text-gray-600 italic">Your dream stay awaits...</p>
  </div>
  <!-- Image Gallery -->
  <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-12">
    <div class="rounded-2xl overflow-hidden shadow-xl hover:scale-105 transition">
      <img src="<%= home.photo %>" class="w-full h-96 object-cover" />
    </div>
    <div class="grid grid-cols-2 gap-4">
      <img src="<%= home.photo %>" class="rounded-xl object-cover h-44 w-full" />
      <img src="<%= home.photo %>" class="rounded-xl object-cover h-44 w-full" />
      <img src="<%= home.photo %>" class="rounded-xl object-cover h-44 w-full" />
      <img src="<%= home.photo %>" class="rounded-xl object-cover h-44 w-full" />
    </div>
  </div>

  <!-- Detail Section -->
  <div class="bg-white shadow-xl rounded-3xl p-8 grid grid-cols-1 md:grid-cols-2 gap-8">

   <!-- Description and Info Section -->
<div class="space-y-6 bg-white/30 backdrop-blur-md p-6 rounded-3xl shadow-lg border border-white/40 transition hover:shadow-2xl hover:scale-[1.01] duration-300">
    
    <!-- Description -->
    <div class="flex items-start gap-4">
      <div class="text-4xl text-blue-500">📋</div>
      <div>
        <h2 class="text-xl md:text-2xl font-bold text-gray-800 mb-1">Description</h2>
        <p class="text-gray-700 text-justify leading-relaxed tracking-wide">
          <%= home.description %>
        </p>
      </div>
    </div>
    
    <!-- Location -->
    <div class="flex items-start gap-4">
      <div class="text-4xl text-green-500">📍</div>
      <div>
        <h2 class="text-xl md:text-2xl font-bold text-gray-800 mb-1">Location</h2>
        <p class="text-gray-700 flex items-center gap-2 text-base md:text-lg">
          🌇 <span><%= home.location %></span>
        </p>
      </div>
    </div>

    <!-- Favorite Button -->
    <div class="pt-4">
      <div class="inline-block">
        <%- include('../partials/favourite') %>
      </div>
    </div>
    <form action="/start-chat/<%= home._id %>" method="POST">
      <button type="submit"
        class="mt-4 bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition">
        💬 Chat with Host
      </button>
    </form>
  </div>
    <!-- Price and Features -->
    <div class="space-y-6">
      <!-- Price -->
      <div class="bg-gradient-to-r from-green-200 to-green-100 rounded-xl p-6 shadow-inner">
        <h3 class="text-xl font-semibold text-gray-800 mb-2 flex items-center">
          <span class="text-green-600 text-3xl mr-2">💰</span>
          Price per night
        </h3>
        <p class="text-4xl font-bold text-green-700">
          $<%= home.price %> <span class="text-base font-normal text-gray-500">/ night</span>
        </p>
      </div>

      <!-- Rating -->
      <div class="bg-gradient-to-r from-yellow-100 to-yellow-50 rounded-xl p-6 shadow-inner">
        <h3 class="text-xl font-semibold text-gray-800 mb-2 flex items-center">
          <span class="text-yellow-500 text-3xl mr-2">⭐</span>
          Rating
        </h3>
        <div class="flex items-center text-yellow-500 text-2xl">
          ★ <span class="ml-2 text-gray-800 text-lg font-semibold"><%= home.rating %> / 5</span>
        </div>
      </div>

      <!-- Booking Button -->
      <a href="/homes/reserve/<%= home._id %>" class="block text-center py-3 rounded-xl bg-blue-600 text-white text-lg font-semibold hover:bg-blue-700 transition">
        Book Now
      </a>
    </div>
  </div>

  <!-- Amenities -->
  <div class="mt-16">
    <h2 class="text-3xl font-bold mb-8 text-center text-gradient shimmer">Exclusive Amenities</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <div class="bg-white rounded-2xl p-6 shadow-lg text-center hover:scale-105 transition">
        <div class="text-4xl mb-2">🏊‍♂️</div>
        <h4 class="text-xl font-bold text-gray-800 mb-1">Swimming Pool</h4>
        <p class="text-gray-500">Infinity rooftop pool with stunning city views.</p>
      </div>
      <div class="bg-white rounded-2xl p-6 shadow-lg text-center hover:scale-105 transition">
        <div class="text-4xl mb-2">🍽️</div>
        <h4 class="text-xl font-bold text-gray-800 mb-1">Gourmet Kitchen</h4>
        <p class="text-gray-500">Fully equipped chef's kitchen with modern appliances.</p>
      </div>
      <div class="bg-white rounded-2xl p-6 shadow-lg text-center hover:scale-105 transition">
        <div class="text-4xl mb-2">🚗</div>
        <h4 class="text-xl font-bold text-gray-800 mb-1">Valet Parking</h4>
        <p class="text-gray-500">24/7 secured and complimentary valet service.</p>
      </div>
    </div>
  </div>
</main>
</body>
</html>
